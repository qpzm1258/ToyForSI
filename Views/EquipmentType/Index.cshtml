@model IEnumerable<ToyForSI.Models.EquipmentType>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.equipmentTypeName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.equipmentTypeRemarks)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.equipmentTypeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.equipmentTypeRemarks)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.equipmentTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.equipmentTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.equipmentTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
